[color]
	ui = auto
[alias]
	graph = log --graph --all --oneline --decorate=full
	unstage = reset HEAD --
	last = log -1 HEAD
	today = log --since=midnight --author='yusekiya' --oneline
[core]
	whitespace = cr-at-eol
	autocrlf = input
[include]
    path = .gitconfig.local
